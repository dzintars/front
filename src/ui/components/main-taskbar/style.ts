import { css } from 'lit-element'

export default css`
  :host {
    display: block;
    background: var(--color-dodgerblue-8d);
    color: var(--color-dodgerblue-8l);
    margin: 0;
  }
  p {
    margin: 0;
  }
`
